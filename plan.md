# 쇼핑몰 주문 → 택배 입고용 엑셀 변환기 — 구현 계획

작성일: 2026-09-16  
상태: **합의 완료 — 모두 A로 구현**  
대상 사용자: 비전공자 서비스 관리자

합의 내용: 2번 파일 9컬럼, 내품명=주문선택사항, 우편번호 5자리, 검증 실패 시 다운로드 불가.

---

## 1. 한 줄 요약

웹 브라우저에서 **쇼핑몰 주문 엑셀(1번)** 을 올리면, 화면에서 **원본과 변환 결과를 나란히 확인**한 뒤, 건수·수량이 맞을 때만 **택배 입고용 엑셀(2번 양식)** 로 다운로드합니다.

원본 파일은 **절대 지우거나 덮어쓰지 않습니다.** 변환은 메모리에서만 하고, 다운로드는 **새 파일**로만 저장합니다.

---

## 2. 제공된 샘플 파일을 보고 확인한 사실

| 구분 | 파일 | 실제 역할 |
| --- | --- | --- |
| 1번 | `2026-09-16_EMP_택배양식.xls` | 쇼핑몰(EMP) 주문 원본. 시트명 `2026-09-16`. **21건**, 주문수량 합계 **39** |
| 2번 | `택배양식20260916.xlsx` | 택배 입고용으로 이미 변환된 **정답 샘플**. 시트명 `Sheet1 (9)`. **21건**, 내품수량 합계 **39** |

요구사항에 적힌 컬럼 이름(`수령인`, `상품명1`, `수량1`, `보내는분성명`, `운임구분` 등)과 **실제 파일 컬럼이 다릅니다.**  
아래 표가 실제 파일 기준입니다.

### 2-1. 1번 원본(EMP) 컬럼 14개

판매사이트명, 판매사이트 상품코드, 주문고유번호, **수령자명**, 상품명, **주문선택사항**, **주문수량**, 추가구매옵션, **수령자휴대폰번호**, **배송지주소**, **배송지우편번호**, **배송메세지**, 배송예정일, 결제일

### 2-2. 2번 택배 양식 컬럼 9개

**받는분성명**, **내품명**, **내품수량**, **받는분전화번호**, **받는분주소(전체, 분할)**, **받는분우편번호**, **배송메세지1**, 발송업체명, 업체요청 메시지

### 2-3. 샘플에서 실제로 옮겨진 값

같은 샘플 1행을 비교하면 다음과 같습니다.

| 원본(1번) | 변환(2번) | 비고 |
| --- | --- | --- |
| 수령자명 → 샘플 고객 | 받는분성명 | 일치 |
| 주문선택사항 → 육회 1kg | 내품명 | **상품명 전체가 아니라 주문선택사항이 들어감** |
| 주문수량 → 1 | 내품수량 | 일치 |
| 수령자휴대폰번호 | 받는분전화번호 | 일치 |
| 배송지주소 | 받는분주소(전체, 분할) | 일치 |
| 배송지우편번호 | 받는분우편번호 | 일치 |
| 배송메세지 → 문 앞 | 배송메세지1 | 일치 |
| (없음) | 발송업체명 / 업체요청 메시지 | 샘플도 비어 있음 |

원본의 긴 `상품명`(예: `국내산 육회 1kg 업소용...`)은 2번 샘플에 **들어가지 않습니다.**

### 2-4. 요구사항 매핑 vs 실제 샘플 (합의 필요)

요구사항에 있던 항목 중, **실제 2번 파일에 없는 것**:

- 받는분기타연락처
- 보내는분성명 / 보내는분전화번호 / 보내는분주소
- 상품명1 / 수량1
- 운임구분 등 기타 택배사 고정 컬럼

**권장안 (이 계획의 기본값):**  
출력 양식은 **2번 샘플 파일의 9개 컬럼을 그대로** 사용합니다.  
요구사항의 일반 택배사 양식(보내는분, 운임구분 등)은 이번 샘플에 없으므로 **넣지 않습니다.**  
나중에 공식 택배사 양식이 추가되면 그때 확장합니다.

---

## 3. 데이터 변환 규칙 (pandas)

원본 행 1건 = 변환 행 1건. 행을 합치거나 나누지 않습니다.

| 원본 컬럼 | 변환 컬럼 | 처리 |
| --- | --- | --- |
| 수령자명 | 받는분성명 | 그대로 복사 |
| 주문선택사항 | 내품명 | 그대로 복사 (샘플과 동일) |
| 주문수량 | 내품수량 | 숫자 그대로 복사 |
| 수령자휴대폰번호 | 받는분전화번호 | 문자 그대로 복사 |
| 배송지주소 | 받는분주소(전체, 분할) | 그대로 복사. 컬럼명 띄어쓰기는 샘플과 동일하게 `(전체, 분할)` |
| 배송지우편번호 | 받는분우편번호 | **5자리 문자열**로 맞춤 (예: `1760` → `01760`). 엑셀이 앞자리 0을 지우는 문제 방지. 주소 내용은 바꾸지 않음 |
| 배송메세지 | 배송메세지1 | 그대로 복사. 비어 있으면 빈칸 유지 |
| (없음) | 발송업체명 | 빈칸 |
| (없음) | 업체요청 메시지 | 빈칸 |

하지 않는 일:

- 원본 파일 저장/덮어쓰기/삭제
- 원본 값 임의 수정(이름 교정, 주소 정리, 행 삭제 등)
- 상품명 전체 문구를 내품명으로 바꾸는 것 (샘플과 다름)

업로드된 원본은 화면에 **읽기 전용 표**로만 보여 줍니다.

---

## 4. 검증 규칙 (다운로드 전, 가장 중요)

화면 상단 요약 카드로 아래를 비교합니다.

| 검증 항목 | 원본 기준 | 변환 기준 | 통과 조건 |
| --- | --- | --- | --- |
| 총 건수 | 행 개수 | 행 개수 | 숫자가 같으면 OK |
| 수량 합계 | `주문수량` 합 | `내품수량` 합 | 숫자가 같으면 OK |

표시 방법:

- 둘 다 같으면 **OK** (초록)
- 하나라도 다르면 **Mismatch** (빨강), 다운로드 버튼은 눌러도 경고를 띄우거나 비활성화

추가 확인(카드 아래 작은 안내):

- 받는분성명 / 전화번호 / 주소가 비어 있는 행이 있으면 건수를 표시 (변환은 하되, 관리자가 표를 보고 판단)

샘플 파일 기준 정답: 건수 21, 수량 합 39 → 둘 다 OK여야 합니다.

---

## 5. 화면 레이아웃 계획

위에서 아래 순서입니다.

```
[제목] 주문 엑셀 → 택배 양식 변환
[안내] 원본은 수정되지 않습니다. 확인 후에만 다운로드하세요.

[업로드 영역]
  드래그 앤 드롭 / 파일 선택
  허용: .xlsx, .xls
  업로드된 파일 이름만 표시 (원본 경로는 건드리지 않음)

----- 파일 올린 뒤에만 아래가 나타남 -----

[요약 카드 4칸]
  원본 건수 | 변환 건수 | 원본 수량합 | 변환 수량합
[검증 배지]
  건수 OK/Mismatch    수량 OK/Mismatch

[탭]
  (1) 원본 데이터          ← 1번 엑셀을 그대로 표로 표시 (수정 불가)
  (2) 변환된 택배 양식     ← 2번 컬럼으로 변환된 표 (수정 불가)
  (3) 좌우 비교            ← 핵심 컬럼만 한 화면에 나란히

[다운로드]
  검증 OK일 때만 활성화 권장
  버튼: "택배 양식 엑셀 다운로드"
  파일명 예: 택배양식_YYYYMMDD_HHMM.xlsx
```

### 5-1. 탭 (3) 좌우 비교에 넣을 열

한 화면에 너무 많은 열을 넣으면 관리자가 보기 어렵습니다. 아래만 비교합니다.

| 왼쪽 (원본) | 오른쪽 (변환) |
| --- | --- |
| 수령자명 | 받는분성명 |
| 주문선택사항 | 내품명 |
| 주문수량 | 내품수량 |
| 수령자휴대폰번호 | 받는분전화번호 |
| 배송지주소 | 받는분주소(전체, 분할) |
| 배송메세지 | 배송메세지1 |

전체 열은 탭 (1), (2)에서 봅니다.

### 5-2. 사용 흐름

1. 브라우저에서 앱을 연다.
2. 1번 엑셀을 드래그해서 올린다.
3. 상단 카드에서 건수·수량이 OK인지 본다.
4. 탭으로 원본 / 변환 / 좌우 비교를 확인한다.
5. 이상이 없으면 다운로드 버튼을 누른다.
6. 받은 파일을 택배사에 업로드한다.

---

## 6. 프로그램 동작 구조 (코딩 시 파일 역할)

구현 합의 후에만 만들 예정인 구조입니다. 지금은 파일만 계획합니다.

```
courier-excel-converter/
  plan.md                 ← 이 문서
  app.py                  ← 화면(업로드, 탭, 카드, 다운로드)
  converter.py            ← 컬럼 매핑 (원본 값은 복사만)
  validator.py            ← 건수·수량 비교
  templates/
    택배양식_컬럼정의.md   ← 2번 양식 컬럼 목록 (참고용)
  requirements.txt        ← streamlit, pandas, openpyxl, xlrd
```

처리 순서:

1. Streamlit 파일 업로더가 파일을 **메모리로만** 읽는다.
2. pandas가 첫 번째 시트를 읽는다. `.xls`는 xlrd, `.xlsx`는 openpyxl.
3. 필수 원본 컬럼이 있는지 검사한다. 없으면 변환하지 않고 안내한다.
4. `converter.py`가 새 DataFrame을 만든다. 원본 DataFrame은 그대로 둔다.
5. `validator.py`가 건수·수량 합을 계산한다.
6. 화면에 표와 카드를 그린다.
7. 다운로드 시에만 변환 DataFrame을 **새 xlsx 바이트**로 만든다.

다운로드 형식: **`.xlsx`** (2번 샘플과 동일). `.xls` 저장은 별도 라이브러리가 필요하고 샘플도 xlsx라서 이번에는 xlsx만 제공합니다.

---

## 7. 기술 선택 (관리자용 설명)

- **Streamlit**: 브라우저에서 파일 올리고 표를 보여주는 파이썬 웹 도구
- **pandas**: 엑셀 행/열을 옮기는 계산 도구
- 로컬 PC에서 `streamlit run app.py` 로 실행. 인터넷에 원본을 올리지 않음

---

## 8. 합의가 필요한 항목

코딩 전에 아래를 정해 주세요.

1. **출력 양식**  
   - A. (권장) 실제 2번 파일 9개 컬럼 그대로  
   - B. 요구사항의 일반 택배사 양식(보내는분, 운임구분, 상품명1 등) — 이 경우 **빈 공식 양식 파일**이 더 필요합니다.

2. **내품명에 넣을 값**  
   - A. (권장, 샘플과 동일) `주문선택사항`  
   - B. 원본 `상품명` 전체

3. **우편번호 앞자리 0**  
   - A. (권장) 변환 파일만 5자리 문자로 맞춤 (`01760`)  
   - B. 샘플처럼 숫자 그대로 (`1760`)

4. **검증 실패 시 다운로드**  
   - A. (권장) 버튼 비활성화  
   - B. 경고만 하고 다운로드는 가능

기본값은 모두 **A** 입니다. 다른 선택이 없으면 A로 구현하겠습니다.

---

## 9. 구현하지 않는 것 (이번 범위 밖)

- 여러 파일 한 번에 올리기
- 화면에서 변환 값 직접 수정
- 택배사 API 연동, 송장번호 발급
- 원본 EMP 파일 자동 저장/백업
- 로그인, 권한, 클라우드 배포

---

## 10. 다음 단계

합의(모두 A)에 따라 `courier-excel-converter` 앱을 구현했습니다.  
실행: 폴더의 `웹앱실행.bat` 또는 `streamlit run app.py`

---

## 11. 2단계 — 운송장번호를 EMP 매출장부에 연결

상태: **사용자 설명 및 제공 샘플 분석 완료 — 기존 앱에 연결**

### 11-1. 입력 파일

1. 택배사에서 내려받은 `파일접수 상세내역_....xlsx`
2. 사이트에서 내려받은 원본 `..._EMP_매출장부.xls`  
   - 원본에는 `운송장번호` 열이 없다고 가정
   - 제공된 검증 샘플에는 사용자가 운송장번호 열을 미리 넣어 정답을 표시함

### 11-2. 샘플에서 확인한 합배송 패턴

- 택배사 상세내역: 17행, 수령자 17명, 운송장번호 17개
- EMP 매출장부: 21행, 수령자 17명
- 합배송 수령자 A: 매출장부 2행에 동일한 운송장번호
- 합배송 수령자 B: 매출장부 4행에 동일한 운송장번호
- 택배사 상세내역의 수령자명별 운송장번호와 매출장부의 수기 정답 21행이 모두 일치

따라서 합배송 수령인은 매출장부에 여러 상품 행이 있더라도 동일한 운송장번호를 각 행에 반복 입력합니다.

### 11-3. 변환 규칙

1. 택배사 상세내역의 `받는분`과 `운송장번호`로 매칭표를 만든다.
2. EMP 매출장부의 `수령자명`을 기준으로 운송장번호를 찾는다.
3. 원본 매출장부의 기존 열·행 순서·값은 바꾸지 않는다.
4. 새 `운송장번호` 열을 **E열(다섯 번째 열)** 에 삽입한다.
5. 합배송으로 같은 수령자명이 여러 행이면 같은 운송장번호를 모든 해당 행에 넣는다.
6. 결과는 원본을 덮어쓰지 않고 새 `.xlsx`로 다운로드한다.

### 11-4. 안전 검증

- 택배사 상세내역에 같은 이름으로 서로 다른 운송장번호가 있으면 `동명이인/중복 충돌`로 판단하고 다운로드를 막는다.
- 택배사 상세내역에 없는 매출장부 수령자가 있으면 `미매칭`으로 표시하고 다운로드를 막는다.
- 빈 수령자명 또는 빈 운송장번호가 있으면 오류로 표시한다.
- 변환 전후 매출장부의 행 수가 같아야 한다.
- 화면에서 원본 매출장부와 운송장번호가 추가된 결과를 다운로드 전에 비교한다.

### 11-5. 화면 흐름

기존 앱에 두 작업을 구분해 제공한다.

- `1단계: 주문 → 택배 양식`
- `2단계: 운송장 → EMP 매출장부`

2단계에서는 택배사 상세내역과 EMP 매출장부를 각각 업로드한 뒤, 전체 행·매칭 행·합배송 행·미매칭/충돌을 요약 카드로 확인하고 새 매출장부를 다운로드한다.

---

## 12. 화면 디자인 — Airbnb-inspired Theme

상태: **사용자 제공 테마로 적용**

- 전체 배경은 흰색, 업로드·보조 영역은 밝은 회색 사용
- 주요 버튼과 선택 강조색은 `#FF385C`
- 기본 글꼴은 Plus Jakarta Sans
- 카드 모서리는 12px, 버튼 모서리는 8px
- 표·입력 요소 테두리를 표시해 업무 화면 구역을 명확하게 구분
- 정상 검증은 초록색, 오류 및 불일치는 오류 색상으로 구분
- 1단계와 2단계 모두 동일한 디자인을 적용

---

## 13. GitHub 비공개 앱 저장소

상태: **사용자 합의 — 비공개 저장소로 게시**

- 프로그램 코드, 테마, 사용 설명서와 가상 데이터 테스트만 게시
- 실제 주문·고객·운송장 Excel 파일은 게시하지 않음
- `.xls`, `.xlsx` 파일을 `.gitignore`로 차단
- 외부 PC에서도 설치하고 실행할 수 있도록 `README.md`에 실행법 기록
- GitHub 기본 브랜치는 `main` 사용

---

## 14. 인터넷 웹앱 배포

상태: **배포 완료 — 비공개 Streamlit Community Cloud 앱**

- 비공개 GitHub 저장소의 `main` 브랜치와 `app.py`를 연결
- 허용된 로그인 사용자만 웹앱에 접근하도록 비공개 설정
- 코드가 GitHub에 업데이트되면 배포 앱에도 반영
- 실제 주문 Excel은 저장소에 포함하지 않음
- 사용자가 올린 파일은 변환 중 메모리에서만 처리하고 별도 저장하지 않음
- 배포 주소: `https://courier-excel-converter.streamlit.app/`

---

## 15. 1단계 택배 필수 입력 규칙

상태: **사용자 합의 — 기존 9개 열을 유지하고 A~F 검증 강화**

| 열 | 출력 필드 | 확정 규칙 |
| --- | --- | --- |
| A | 받는분성명 | 2글자 이상 필수. 원본 성명이 1글자이면 변환 결과에 점(`.`)을 붙임 (`집` → `집.`) |
| B | 내품명 | 기존 합의대로 `주문선택사항`을 정확히 복사하며 빈 값은 허용하지 않음 |
| C | 내품수량 | 숫자로 변환 가능한 값만 허용 |
| D | 받는분전화번호 | 하이픈이 포함된 일반 연락처 또는 안심번호만 허용. 잘못된 형식은 임의 수정하지 않고 다운로드 차단 |
| E | 받는분주소(전체, 분할) | 기존 샘플 열 이름을 유지하고 전체 주소를 복사. 빈 값은 허용하지 않음 |
| F | 받는분우편번호 | 변환 결과를 5자리 문자열로 맞추고, 5자리가 아니면 다운로드 차단 |

- G~I(`배송메세지1`, `발송업체명`, `업체요청 메시지`)는 기존 규칙 유지
- 자동 보정된 1글자 성명 건수를 화면에 알림
- 필수 입력 오류의 항목별 건수를 화면에 표시
- 건수·수량 검증과 A~F 규칙을 모두 통과해야 다운로드 가능
